Sandbox database credentials:

 - name: sandbox
 - username: sandbox
 - password: sandbox

Create Yaml:
 - ./vendor/bin/openapi src -o docum.yaml

To initiate the swagger:
 - open-swagger-ui ./docum.yaml --open
